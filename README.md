# Personal Portfolio Page

Available here: https://leweixu.com/

Plain static HTML and CSS. Deployed on Vercel from `main`.

To preview locally: `python3 -m http.server -d public` then open http://localhost:8000.
